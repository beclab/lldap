query GetUserDetails($id: String!) {
  user(userId: $id) {
    id
    userIndex
    email
    displayName
    firstName
    lastName
    avatar
    creationDate
    uuid
    groups {
      id
      displayName
    }
  }
}
